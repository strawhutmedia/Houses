{
  "name": "equityscout-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Shared-favorites board sync for EquityScout (secret-link boards, no accounts)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": { "node": ">=20" }
}
